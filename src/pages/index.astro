---
import "../styles/global.css";

const siteTitle = "Dokumint";
const siteDescription =
  "A modern MSP documentation platform, built by MSPs for MSPs.";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>{siteTitle}</title>
    <meta name="description" content={siteDescription} />

		<link rel="icon" href="/favicon/favicon.ico" />
		<link rel="apple-touch-icon" href="/favicon/apple-touch-icon.png" />
		<link rel="manifest" href="/favicon/site.webmanifest" />

    <meta name="robots" content="index, follow" />
  </head>

  <body class="bg-white text-gray-900 antialiased
             dark:bg-gray-950 dark:text-gray-100">

      <!-- Header -->
    <header class="border-b border-gray-200 dark:border-gray-800">
      <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
        <div class="flex items-center gap-3">
          <img
            src="/images/dokumint-logo.svg"
            alt="Dokumint"
            class="h-7 w-auto"
          />
          <span class="text-xl font-semibold tracking-tight">
            Dokumint
          </span>
        </div>

        <nav class="flex items-center gap-6 text-sm">
          <a href="/docs" class="text-gray-500 hover:text-gray-900 dark:hover:text-gray-100">
            Docs
          </a>
          <a href="/github" class="text-gray-500 hover:text-gray-900 dark:hover:text-gray-100">
            GitHub
          </a>
          <a
            href="/login"
            class="px-4 py-2 rounded-lg bg-gray-900 text-white dark:bg-gray-100 dark:text-gray-900 hover:opacity-90"
          >
            Get Started
          </a>
        </nav>
      </div>
    </header>

  <main class="max-w-4xl mx-auto px-6 py-16 space-y-14">

			    <!-- Hero -->
    <section class="max-w-6xl mx-auto px-6 py-20">
      <div class="max-w-3xl">
        <h1 class="text-5xl font-semibold leading-tight mb-6">
          Documentation built by MSPs,<br />
          not product managers
        </h1>

        <p class="text-xl text-gray-500 dark:text-gray-400 leading-relaxed mb-8">
          Doku<span class="org-mint">m</span>int is a modern MSP documentation system designed around how
          technicians actually work — not how vendors want to sell.
        </p>

        <div class="flex gap-4">
          <a
            href="/login"
            class="px-6 py-3 rounded-lg bg-gray-900 text-white dark:bg-gray-100 dark:text-gray-900 hover:opacity-90"
          >
            Start Building
          </a>

          <a
            href="/demo"
            class="px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-700 hover:bg-gray-100 dark:hover:bg-gray-900"
          >
            View Demo
          </a>
        </div>
      </div>
    </section>

			<div class="h-px bg-gray-200 dark:bg-gray-800 my-12"></div>

			    <section class="max-w-6xl mx-auto px-6 py-16">
      <div class="grid md:grid-cols-2 gap-12">
        <div>
          <h2 class="text-3xl font-semibold mb-4">
            Why Dokumint exists
          </h2>
          <p class="text-gray-500 dark:text-gray-400 leading-relaxed">
            Most documentation platforms grew by bolting features onto an old
            core. Permissions came later. Structure came later. Multi-tenancy
            came even later.
          </p>
        </div>

        <div>
          <p class="text-gray-500 dark:text-gray-400 leading-relaxed">
            Dokumint started clean. Tenants, roles, permissions, and data
            boundaries were treated as first-class concerns from day one.
          </p>
        </div>
      </div>
    </section>

				
	    <section class="max-w-6xl mx-auto px-6 py-16">
      <h2 class="text-3xl font-semibold mb-12">
        Included by default
      </h2>

      <div class="grid md:grid-cols-3 gap-8">
        <div class="p-6 border border-gray-200 dark:border-gray-800 rounded-xl">
          <h3 class="font-semibold mb-2">Proper multi-tenancy</h3>
          <p class="text-sm text-gray-500 dark:text-gray-400">
            Not an afterthought
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800 rounded-xl">
          <h3 class="font-semibold mb-2">Role-based access control</h3>
          <p class="text-sm text-gray-500 dark:text-gray-400">
            Built in from day one
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800 rounded-xl">
          <h3 class="font-semibold mb-2">Structured assets</h3>
          <p class="text-sm text-gray-500 dark:text-gray-400">
            Typed, queryable, predictable
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800 rounded-xl">
          <h3 class="font-semibold mb-2">Encrypted passwords</h3>
          <p class="text-sm text-gray-500 dark:text-gray-400">
            Security at the core
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800 rounded-xl">
          <h3 class="font-semibold mb-2">Revision history</h3>
          <p class="text-sm text-gray-500 dark:text-gray-400">
            Every change tracked
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800 rounded-xl">
          <h3 class="font-semibold mb-2">API-first design</h3>
          <p class="text-sm text-gray-500 dark:text-gray-400">
            Automate everything
          </p>
        </div>
      </div>
    </section>

				
	    <section class="max-w-6xl mx-auto px-6 py-16">
      <div class="bg-gray-100 dark:bg-gray-900 rounded-xl p-10">
        <h2 class="text-3xl font-semibold mb-4">
          Status
        </h2>
        <p class="text-gray-600 dark:text-gray-400 leading-relaxed mb-2">
          Dokumint is actively developed and used in real MSP environments.
        </p>
        <p class="text-gray-600 dark:text-gray-400 leading-relaxed">
          We’re building what actually gets used — not chasing checkbox parity.
        </p>
      </div>
    </section>

			
		</main>
		<footer class="border-t border-gray-200 dark:border-gray-800 mt-20">
      <div class="max-w-6xl mx-auto px-6 py-12 text-sm text-gray-500 dark:text-gray-400">
        © {new Date().getFullYear()} Doku<span class="org-mint">m</span>int. Built for MSPs, by MSPs.
      </div>
    </footer>

	</body>
</html>
